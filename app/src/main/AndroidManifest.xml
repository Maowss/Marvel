<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.etecia.marvel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FerroActivity"></activity>
        <activity android:name=".ProfessorActivity" />
        <activity android:name=".MagnetoActivity" />
        <activity android:name=".AranhaActivity" />
        <activity android:name=".ThorActivity" />
        <activity android:name=".LukeActivity" />
        <activity android:name=".LoganActivity" />
        <activity android:name=".GrootActivity" />
        <activity android:name=".DareDevilActivity" />
        <activity android:name=".CapitaoActivity" />
        <activity android:name=".MarvelActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>